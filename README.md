Install MCP server by adding JSON code to your JSON config

```json
{
  "mcpServers": {
    "server": {
      "command": "uvx",
      "args": [
        "--from",
        "git+https://github.com/PiotrDabrowski232/MCPServers.git"
        "chess"
      ]
    }
  }
}


```
